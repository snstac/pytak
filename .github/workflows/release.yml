name: release

on:
  push:
    tags:
      - 'v*.*.*'
    branches: [ main, add_packaging ]

jobs:
  build-artifact:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'
      - name: Install dependencies
        run: |
          sudo apt install python3-all debhelper -y
          python -m pip install --upgrade pip
          pip install setuptools wheel twine stdeb
      - name: Build and publish
        env:
          TWINE_USERNAME: ${{ secrets.PYPI_USERNAME }}
          TWINE_PASSWORD: ${{ secrets.PYPI_PASSWORD }}
        run: |
          python setup.py sdist bdist_wheel
          python setup.py --command-packages=stdeb.command bdist_deb --build-depends dh-python

      - uses: actions/upload-artifact@v2
        with:
          name: artifact-${{ matrix.os }}
          path: |
            dist/*.tar.gz
            dist/*.zip

      - uses: actions/upload-artifact@v2
        with:
          name: artifact-deb
          path: |
            */*.deb
            *.deb
